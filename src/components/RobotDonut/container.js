import {connect} from 'react-redux'
import {RobotDonut} from './RobotDonut'

const mapActionCreators = {
};

const mapStateToProps = (state) => ({
});

export const RobotDonutContainer = connect(mapStateToProps, mapActionCreators)(RobotDonut);
